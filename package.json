{
  "name": "baucua-online",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "nx run-many -t dev",
    "dev:client": "nx run @baucua/client:dev",
    "dev:server": "nx run @baucua/server:dev",
    "build": "nx run-many -t build",
    "clean": "rm -rf packages/*/dist packages/*/build node_modules/.cache"
  },
  "devDependencies": {
    "nx": "^21.0.0",
    "typescript": "^5.7.0"
  }
}
